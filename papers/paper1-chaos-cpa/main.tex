\documentclass[conference,a4paper]{IEEEtran}
\IEEEoverridecommandlockouts
\usepackage{cite}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{url}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning}
\usepackage{amsthm}
\usepackage{algorithm}
\usepackage{algpseudocode}
\newtheorem{proposition}{Proposition}

% Placeholders (PH of a key) are replaced by scripts/fill_tex.py from results/*.json.
\newcommand{\PH}[1]{\textcolor{red}{\texttt{#1}}}

\begin{document}

\title{An Equivalent-Key Chosen-Plaintext Break of a\\
2025 Chaos-Based Image Cipher}

\author{\IEEEauthorblockN{Anonymous (single author)}
\IEEEauthorblockA{Affiliation withheld for review \\ \texttt{author@example.org}}}

\maketitle
\thispagestyle{empty}
\pagestyle{empty}

\begin{abstract}
We cryptanalyse a recently published image-encryption scheme that combines Lorenz/logistic chaotic
maps with Fisher--Yates block shuffling (Jain \emph{et al.}, \emph{Optik}, 2025). We observe that the
scheme's keystream and both permutations depend only on the secret key and never on the image, so the
whole encryption is a fixed, plaintext-independent map. We reconstruct the cipher from its
description---matching its reported near-ideal entropy (e.g.\ \PH{cam_ent_cipher} on the Cameraman
image) and near-zero adjacent-pixel correlation---and then break it: an all-ones probe recovers the
per-position keystream and a few index-digit probes recover the permutation, so an equivalent key is
obtained in \PH{cam_cpt} chosen plaintexts and any ciphertext is decrypted pixel-exactly in
\PH{cam_attack_s}\,s without the key. We further show the scheme's headline differential metric is
illusory: because it applies no plaintext diffusion, a one-pixel change alters exactly one ciphertext
pixel, giving a true NPCR of \PH{cam_npcr1px}\,\% (i.e.\ $100/MN$), not the $\approx$99.6\,\% reported
(that figure merely reflects the difference between unrelated images, here \PH{npcr_unrelated}\,\%).
We conclude with the standard fixes.
\end{abstract}

\begin{IEEEkeywords}
cryptanalysis, image encryption, chaotic cipher, chosen-plaintext attack, equivalent key, NPCR
\end{IEEEkeywords}

\section{Introduction}
Chaos-based image ciphers appear in the literature at a high rate, and a recurring lesson from the
cryptanalysis side is that a large fraction of them succumb to the same structural attack. Most follow
a permutation--diffusion template: a confusion stage that rearranges pixels and a diffusion stage that
masks pixel values with a chaotic keystream. When that keystream is a function of the secret key alone
and not of the image, the entire cipher collapses to a \emph{fixed} permutation--substitution map,
and an \emph{equivalent key}---a representation that decrypts every ciphertext under the same secret
key---can be recovered from a handful of chosen
plaintexts~\cite{li2019attacker,li2018entropy,arroyo2009,li2011optimal}. The defence is equally
well known: the keystream must depend on the plaintext, and the cipher must actually
diffuse~\cite{alvarez2006}.

A second, persistent problem is methodological. Authors validate schemes with statistical scores---%
Shannon entropy, adjacent-pixel correlation, and the differential metrics NPCR and UACI---and treat
near-ideal values as evidence of security. None of these scores measures resistance to a
chosen-plaintext attack, and, as we show, all of them can be near-ideal for a cipher that is broken in
seconds.

This paper applies both lessons to a 2025 scheme~\cite{jain2025lleo} (which we abbreviate LLEO) built
from the Lorenz~\cite{lorenz1963} and logistic maps with Fisher--Yates block shuffling. Our
contributions are:
\begin{itemize}
\item We identify that LLEO is entirely key-driven and therefore a fixed monomial map over
$\mathbb{Z}_{256}$ (Section~\ref{sec:target}--\ref{sec:crypt}).
\item We give an equivalent-key chosen-plaintext attack that recovers the full map in
$1+\lceil\log_{256} MN\rceil$ chosen plaintexts---\PH{cam_cpt} for a $512\times512$ image---and
decrypts any image pixel-exactly without the key (Section~\ref{sec:crypt}).
\item We reproduce LLEO, confirm it matches the paper's reported statistics, and then show those
statistics coexist with \emph{zero} plaintext diffusion: the true one-pixel NPCR is
\PH{cam_npcr1px}\,\%, not 99.6\,\% (Section~\ref{sec:results}).
\end{itemize}

\section{Related work}
The equivalent-key methodology for permutation--diffusion ciphers is well established. Arroyo
\emph{et al.}~\cite{arroyo2009} broke a ``total shuffling'' scheme by recovering its key-only
keystream; Li \emph{et al.}~\cite{li2018entropy} recovered an equivalent key for an entropy-based
cipher; and the survey of Li \emph{et al.}~\cite{li2019attacker} catalogues a year of such breaks,
nearly all exploiting plaintext-independent diffusion. For the confusion stage alone, Li and
Lo~\cite{li2011optimal} established the optimal number of known/chosen plaintexts needed to invert a
permutation-only multimedia cipher, of order $\log_{L} (MN)$ for $L$ intensity levels---the same
logarithmic regime our attack achieves; the general quantitative theory of permutation-only
cryptanalysis~\cite{Li2008general} bounds this effort, and recent chosen-plaintext breaks continue to
recover equivalent keys from a handful of images~\cite{Ma2020cryptanalysis,Zhao2025cryptanalysis},
confirming the vulnerability remains a live concern. Alvarez and Li~\cite{alvarez2006} set out basic requirements
for chaos-based cryptosystems, including plaintext-dependent keystreams and sensitivity to the
plaintext; LLEO violates these. The differential metrics we scrutinise were standardised by Wu
\emph{et al.}~\cite{wu2011npcr}, who also note that NPCR/UACI quantify sensitivity to a one-pixel
plaintext change---precisely the quantity LLEO fails to deliver despite its reported value.

The pattern these works expose is structural rather than incidental. A permutation--diffusion cipher
whose confusion and diffusion are both fixed by the key is, as a function of the plaintext, an affine
or monomial map; composing more such stages yields another map of the same form, so neither extra
rounds nor extra chaotic maps add security against an attacker who can query the
encryption~\cite{li2019attacker,li2011optimal}. The decisive design choice is therefore binary: does
any key material depend on the image? Schemes that answer ``no''---including LLEO, and the block- and
shuffling-based constructions analysed in~\cite{arroyo2009,li2018entropy}---are broken by recovering an
equivalent key, while schemes that bind the keystream to a per-image value (a hash, or a feedback
chain) resist it. Our analysis of LLEO is a direct instance of this dichotomy, and our positive
control (Section~\ref{sec:results}) demonstrates both sides of it on the same construction.

\section{The target cipher}\label{sec:target}
LLEO encrypts an $M\times N$ grayscale image $X$ in three key-driven steps.

\emph{(1) Substitution.} $X$ is split into 16 horizontal strips. The eight odd strips are combined
with a keystream $k_{\mathrm{log}}$ produced by the logistic map $x_{n+1}=r\,x_n(1-x_n)$, and the
eight even strips with a keystream $k_{\mathrm{lor}}$ produced by the Lorenz system
$\dot x=\sigma(y-x),\ \dot y=x(\rho-z)-y,\ \dot z=xy-\beta z$, through an invertible per-position
operation; decryption uses the modular ``conjugate'' of the keystream, i.e.\ a multiplicative inverse.

\emph{(2) Permutation 1.} The 16 horizontal strips are reordered by a Fisher--Yates shuffle.

\emph{(3) Permutation 2.} The result is divided into 16 vertical strips and shuffled again, giving the
ciphertext.

The four ``decryption keys'' are the two keystreams ($d_1,d_2$) and the two shuffle index lists
($d_3,d_4$). The chaotic initial conditions are derived from the secret key and the shuffles are
seeded from it; \emph{no quantity depends on the image $X$}. Although the scheme is repeatedly called
``asymmetric,'' the same four keys both encrypt and decrypt: it is a symmetric cipher.

\begin{figure}[t]
\centering
\footnotesize
\begin{tikzpicture}[>=Stealth,
  box/.style={draw, rounded corners, minimum height=5.5mm, inner xsep=3pt}, node distance=4mm]
  \node[box] (x) {$X$};
  \node[box, right=of x] (k) {$\times K$};
  \node[box, right=of k] (h) {FY rows};
  \node[box, right=of h] (v) {FY cols};
  \node[box, right=of v] (c) {$C$};
  \foreach \a/\b in {x/k,k/h,h/v,v/c} \draw[->] (\a)--(\b);
  \node[box, below=7mm of h, text width=0.84\columnwidth, align=center, fill=black!5] (atk)
    {attack: $\mathbf{1}\!\to\!K$; \ $\lceil\log_{256}n\rceil$ digit probes $\to s$; \ then
     $X[s(j)]=K[s(j)]^{-1}C[j]$};
  \draw[->, dashed] (c) |- (atk);
\end{tikzpicture}
\caption{LLEO encryption (top): key-only multiplicative substitution $\times K$, then two
Fisher--Yates block shuffles. The equivalent-key attack (bottom) recovers the multiplier $K$ and source
map $s$ from a few chosen plaintexts and inverts the cipher without the key.}
\label{fig:schema}
\end{figure}

\section{Cryptanalysis}\label{sec:crypt}
\subsection{Threat model}
We work in the standard chosen-plaintext setting under Kerckhoffs's principle: the attacker knows the
algorithm and has oracle access to encryption under a fixed unknown key, and seeks an \emph{equivalent
key}---a procedure that decrypts any ciphertext of the probed size---rather than the chaotic secret seed
itself. Because the cipher is plaintext-independent (below), the same equivalent key is also recoverable
from enough known plaintext--ciphertext pairs~\cite{zhang2018improved}, and the lack of diffusion leaks
structure even ciphertext-only; we present the chosen-plaintext version as it is minimal and
constructive.

\subsection{Structural observation}
Index the image as a length-$n$ vector, $n=MN$. Each LLEO step is linear over $\mathbb{Z}_{256}$ and
fixed by the key: substitution multiplies coordinate $p$ by a fixed unit $K[p]$ (odd, hence invertible
mod 256), and the two block shuffles compose into one position permutation $P$. Therefore
\begin{equation}\label{eq:monomial}
C[j] \;=\; K[s(j)]\cdot X[s(j)] \pmod{256},\qquad s=P^{-1},
\end{equation}
a fixed \emph{monomial} map: every ciphertext pixel is a single plaintext pixel scaled by a fixed unit
and relocated. Two consequences are immediate. There is no additive constant, since
$E(\mathbf{0})=\mathbf{0}$. And there is no diffusion: $C[j]$ depends on exactly one input coordinate
$s(j)$.

\begin{proposition}\label{prop:monomial}
If a permutation--diffusion cipher's keystream and permutations depend only on the key, it realises a
fixed \emph{affine} map of the plaintext: the monomial \eqref{eq:monomial} with each $K[p]$ a unit in
$\mathbb{Z}_{256}$ when the substitution is a modular multiply, or an affine map $C=Mx\oplus b$ over
$\mathrm{GF}(2)$ (with $b=E(\mathbf{0})$ possibly nonzero) when the substitution is XOR-based; composing
any number of such rounds yields a map of the same form. In either case it is recovered from
$1+\lceil\log_{256}n\rceil$ chosen plaintexts: one all-ones (resp.\ all-zeros) query fixes the
substitution at every output position, and $\lceil\log_{256}n\rceil$ index-digit queries fix the
permutation $s$.
\end{proposition}
\begin{proof}[Proof sketch]
Each stage---multiplication by the key-derived $K$ and the two block shuffles---is a fixed function of
the key applied to pixel values or positions, so any composition is a fixed key-only map; over
$\mathbb{Z}_{256}$ this is the monomial \eqref{eq:monomial}. The keystream is forced odd, so $K[p]$ is a
unit and $x\mapsto K[p]\,x$ is a bijection with inverse $K[p]^{-1}$. Querying $X=\mathbf{1}$ gives
$C[j]=K[s(j)]$; querying the $t$-th base-256 digit of the index gives
$C[j]=K[s(j)]\,\mathrm{digit}_t(s(j))$, and dividing by the recovered $K[s(j)]$ yields
$\mathrm{digit}_t(s(j))$; collecting all digits reconstructs $s$. If instead the substitution is XOR
with a key-only keystream, the same argument over $\mathrm{GF}(2)$ gives an affine map $C=Mx\oplus b$
with $b=E(\mathbf{0})$: the all-zeros query returns $b$ and the index-digit queries return $s$, exactly
as in the CBC-like XOR cipher we break in Section~\ref{sec:crypt}.
\end{proof}

\subsection{Equivalent-key recovery}
Because $K$ and $s$ are fixed, two kinds of chosen plaintext suffice.

\emph{(a) Multiplier, one query.} Encrypting the all-ones image $\mathbf{1}$ gives, by
\eqref{eq:monomial}, $C[j]=K[s(j)]\cdot 1=K[s(j)]$: the exact multiplier acting at every output
position.

\emph{(b) Permutation, $\lceil\log_{256} n\rceil$ queries.} Let $P_t$ be the plaintext whose pixel $i$
holds the $t$-th base-256 digit of its index $i$. Then $C[j]=K[s(j)]\cdot \mathrm{digit}_t(s(j))$;
multiplying by the already-known $K[s(j)]^{-1}$ yields $\mathrm{digit}_t(s(j))$. Collecting all
$\lceil\log_{256} n\rceil$ digits reconstructs $s(j)$ for every $j$, hence the full permutation.

The recovered pair $(s,K)$ is an equivalent key; any ciphertext is inverted by
$X[s(j)] = K[s(j)]^{-1}\,C[j]$. The total cost is
\begin{equation}
1+\big\lceil\log_{256} n\big\rceil \text{ chosen plaintexts},
\end{equation}
i.e.\ \PH{cam_cpt} for a $512\times512$ image, independent of the nominal key length.

Concretely, a $512\times512$ image needs four queries---one all-ones image and three index-digit
images---after which Algorithm~\ref{alg:attack} returns the full map in a single pass.
Table~\ref{tab:complexity} states the complexity and compares it to the optimal permutation-only bound
of Li and Lo~\cite{li2011optimal}: our query count is order-optimal and the cost is independent of the
cipher's nominal key length, so the advertised keyspace is irrelevant---the equivalent key bypasses key
recovery entirely~\cite{Jolfaei2016permutation}.

\begin{algorithm}[t]
\caption{Equivalent-key recovery for LLEO}
\label{alg:attack}
\begin{algorithmic}[1]
\State $\hat{K} \gets \mathrm{Enc}(\mathbf{1})$ \Comment{$\hat{K}[j]=K[s(j)]$: multiplier at output $j$}
\For{$t \gets 0$ \textbf{to} $\lceil\log_{256} n\rceil-1$}
  \State $C \gets \mathrm{Enc}(P_t)$ \Comment{$P_t[i]={}$ the $t$-th base-256 digit of index $i$}
  \State $d_t[j] \gets C[j]\cdot \hat{K}[j]^{-1} \bmod 256$ \textbf{for all} $j$
\EndFor
\State $s[j] \gets \sum_t d_t[j]\,256^{t}$ \Comment{source index for each output $j$}
\State \Return $\mathrm{Dec}(C):\ X[s(j)] \gets \hat{K}[j]^{-1} C[j] \bmod 256$
\end{algorithmic}
\end{algorithm}

\begin{table}[t]
\centering
\caption{Attack complexity vs.\ the optimal permutation-only bound ($n=MN$ pixels, $L=256$ levels).}
\label{tab:complexity}
\begin{tabular}{lcc}
\toprule
 & This attack & Optimal perm.-only~\cite{li2011optimal} \\
\midrule
Data (chosen plaintexts) & $1+\lceil\log_{256} n\rceil$ & $\lceil\log_{L} n\rceil$ \\
Time  & $O(n\lceil\log_{256} n\rceil)$ & $O(n\lceil\log_{L} n\rceil)$ \\
Memory & $O(n)$ & $O(n)$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Why extra structure does not help}
A composition of fixed key-only maps is again a fixed key-only map, so adding rounds, more chaotic
maps, or further block shuffles leaves \eqref{eq:monomial} intact (only $K$ and $s$ change) and the
same attack applies unchanged. The attack is also a \emph{known}-plaintext attack when the available
plaintexts happen to span the index digits; chosen plaintexts only make the queries explicit and
minimal. The single property that would defeat it is a keystream that depends on $X$.

\section{Experimental results}\label{sec:results}
We reconstructed LLEO from its description (reimplemented from scratch, no code reused) and ran it on
four standard $512\times512$ images.

\subsection{Reproduction}
Table~\ref{tab:repro} shows the reconstruction reproduces LLEO's claimed statistics: cipher entropy
approaches the ideal 8 (Cameraman \PH{cam_ent_cipher}, up from the plaintext's \PH{cam_ent_plain}) and
adjacent-pixel correlation drops to near zero (\PH{cam_corr_cipher}). Fig.~\ref{fig:hist} shows the
flat cipher histogram and Fig.~\ref{fig:corr} the destroyed correlation. By the usual yardsticks the
cipher looks strong.

\begin{table}[t]
\centering
\caption{Reconstruction statistics and attack outcome (512$\times$512). Every image is recovered
pixel-exactly without the key.}
\label{tab:repro}
\begin{tabular}{lccccc}
\toprule
Image & $H$ plain & $H$ cipher & corr.\ cipher & chosen PT & broken \\
\midrule
Cameraman & \PH{cam_ent_plain} & \PH{cam_ent_cipher} & \PH{cam_corr_cipher} & \PH{cam_cpt} & yes \\
Moon      & \PH{moon_ent_plain} & \PH{moon_ent_cipher} & \PH{moon_corr_cipher} & \PH{moon_cpt} & yes \\
Gravel    & \PH{grv_ent_plain} & \PH{grv_ent_cipher} & \PH{grv_corr_cipher} & \PH{grv_cpt} & yes \\
Brick     & \PH{brk_ent_plain} & \PH{brk_ent_cipher} & \PH{brk_corr_cipher} & \PH{brk_cpt} & yes \\
\bottomrule
\end{tabular}
\end{table}

\subsection{The break}
The attack of Section~\ref{sec:crypt} recovers the equivalent key in \PH{cam_cpt} chosen plaintexts and
decrypts a fresh ciphertext pixel-exactly in \PH{cam_attack_s}\,s (Cameraman), using no
key.\footnote{Wall-clock, single core. The $512^2$ entry in Table~\ref{tab:scale} (\PH{scale512_s}\,s,
a random image) differs only by run-to-run variation.}
Fig.~\ref{fig:break} shows the original, the LLEO ciphertext, and the key-free recovery, which is
identical to the original.

\begin{figure*}[t]
\centering
\includegraphics[width=0.92\textwidth]{figures/break_grid.pdf}
\caption{LLEO original, ciphertext, and key-free recovery for four standard $512\times512$ images. Each
recovery uses only \PH{cam_cpt} chosen plaintexts and no key, and is bit-identical to the original.}
\label{fig:break}
\end{figure*}

\subsection{Cost scales logarithmically}
Because the attack needs only $1+\lceil\log_{256} MN\rceil$ chosen plaintexts, its cost is nearly flat
as the image grows (Table~\ref{tab:scale}): \PH{scale128_cpt} plaintexts at $128^2$,
\PH{scale512_cpt} at $512^2$, and still \PH{scale1024_cpt} at $1024^2$, each recovered exactly in well
under a second. The key length the scheme advertises does not enter this cost.

\begin{table}[t]
\centering
\caption{Attack cost vs.\ image size (random plaintext, single core).}
\label{tab:scale}
\begin{tabular}{lccc}
\toprule
Size & chosen PT & time (s) & recovered \\
\midrule
$128\times128$   & \PH{scale128_cpt}  & \PH{scale128_s}  & exact \\
$256\times256$   & \PH{scale256_cpt}  & \PH{scale256_s}  & exact \\
$512\times512$   & \PH{scale512_cpt}  & \PH{scale512_s}  & exact \\
$1024\times1024$ & \PH{scale1024_cpt} & \PH{scale1024_s} & exact \\
\bottomrule
\end{tabular}
\end{table}

\subsection{The differential metric is illusory}
LLEO reports NPCR $\approx$99.6\,\% as evidence of differential-attack resistance. But by
\eqref{eq:monomial} flipping one plaintext pixel changes exactly one ciphertext pixel, so the true
one-pixel NPCR is $100/MN=\PH{cam_npcr1px}\,\%$ (UACI \PH{cam_uaci1px}\,\%)---five orders of magnitude
below the reported value. The 99.6\,\% figure instead measures the difference between \emph{unrelated}
images, which for LLEO is \PH{npcr_unrelated}\,\%; this is achieved by any cipher with a near-uniform
output and says nothing about chosen-plaintext security. Table~\ref{tab:claims} contrasts each claimed
property with what the cipher actually provides. Finally, the chaotic keystreams are themselves
non-uniform: a chi-square test rejects uniformity for the logistic keystream
($\chi^2=\PH{chi2_logistic}$, $p<10^{-300}$, numerically zero).

\begin{table}[t]
\centering
\caption{Claimed vs.\ actual security of LLEO.}
\label{tab:claims}
\begin{tabular}{lcc}
\toprule
Property & Reported & Actual \\
\midrule
Entropy (Cameraman)        & $\approx$7.997 & \PH{cam_ent_cipher} (genuine) \\
NPCR (1-pixel change)      & $\approx$99.6\,\% & \PH{cam_npcr1px}\,\% \\
Differential resistance    & yes & no (no diffusion) \\
Chosen-plaintext security  & implied & broken in \PH{cam_cpt} queries \\
Key type                   & ``asymmetric'' & symmetric \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.7\columnwidth]{figures/cipher_hist.pdf}
\caption{Cipher histogram (Cameraman): near-uniform, yet the cipher is trivially broken.}
\label{fig:hist}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.92\columnwidth]{figures/correlation_scatter.pdf}
\caption{Adjacent-pixel correlation, plain vs.\ cipher (Cameraman).}
\label{fig:corr}
\end{figure}

\subsection{The attack is not specific to LLEO}
The same principle breaks any key-only permutation--diffusion cipher. As a second, structurally
distinct example we take the canonical construction with a CBC-like XOR diffusion chain,
$C[i]=X_{\pi}[i]\oplus k[i]\oplus C[i-1]$---representative of a large published family and unlike
LLEO's multiplicative monomial form. With a key-only keystream this map is affine over
$\mathrm{GF}(2)$ (the identity $E(a)\oplus E(b)\oplus E(\mathbf 0)=E(a\oplus b)$ holds at one
\emph{and} three rounds in our checks), and the equivalent-key CPA recovers it exactly in
\PH{gen_cpt} chosen plaintexts (here at $128\times128$: one all-zeros query plus
$\lceil\log_{256}n\rceil{=}2$ digit probes; the count follows the same $1+\lceil\log_{256}n\rceil$ rule,
so a $512\times512$ instance would need four). The identical procedure fails against a plaintext-hash-seeded version
of the \emph{same} structure, reinforcing that what matters is the keystream's dependence on the
image---not the algebra or the round count.

\section{Discussion}
LLEO illustrates two persistent failure modes. First, \emph{good statistics are not security}: a flat
histogram, near-ideal entropy, near-zero correlation, and a large inter-image NPCR are all satisfied
by our fixed, key-only map, which we nonetheless break in \PH{cam_cpt} queries. The community
guidance to test plaintext sensitivity directly~\cite{alvarez2006,wu2011npcr} is exactly what would
have caught this. Second, \emph{naming is not architecture}: labelling a symmetric construction
``asymmetric'' adds no security.

The remedies are standard. (i) Make the keystream depend on the plaintext---e.g.\ derive the chaotic
initial conditions from a cryptographic hash (SHA-256) of the image, so that a different plaintext
yields an unrelated keystream and the all-ones/digit probes no longer reveal a reusable equivalent
key. (ii) Add genuine diffusion, e.g.\ a chaining rule $C[i]=f(X[i],k[i],C[i-1])$, so that one
plaintext change propagates and the true NPCR approaches its ideal. With either defence in place the
map of \eqref{eq:monomial} is no longer fixed across images and the attack of Section~\ref{sec:crypt}
fails. We verified this directly: the identical attack recovers \PH{keyonly_match_pct}\,\% of pixels
against the key-only cipher but only \PH{defended_match_pct}\,\% (chance level) once the keystream is
seeded from a SHA-256 hash of the image.

\emph{Scope and disclosure.} We recover an equivalent key that decrypts any ciphertext of the probed
size, not the underlying chaotic secret seed---the standard, defensible claim in this
literature~\cite{li2019attacker}. Consistent with responsible-disclosure practice, the authors of the
target scheme will be notified before camera-ready, and our reproduction and attack code is released to
support verification.

\section{Conclusion}
A 2025 chaos-based image cipher is completely broken by an equivalent-key chosen-plaintext attack in
\PH{cam_cpt} chosen plaintexts and a fraction of a second, despite near-ideal entropy and a reported
NPCR of $\approx$99.6\,\%. The break follows directly from the cipher being key-only; its differential
metric, measured correctly, is \PH{cam_npcr1px}\,\%. We recommend plaintext-dependent keystreams and
real diffusion, and that statistical scores accompany---rather than replace---an explicit
chosen-plaintext security argument.

\section*{Acknowledgment}
The author used a large language model (Anthropic Claude) to assist with code, reproduction, and
editing. All claims were verified by the author; the AI system is not an author. Generative-AI use is
disclosed per IEEE policy.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
